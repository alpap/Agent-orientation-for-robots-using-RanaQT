function move(prX, prY,newX, newY)
  
  if prX > newX then
    prX=prX-1
  
  elseif prX < newX then
    prX=prX-+
  end
  
  if prX > newX then
    prX=prX-1
  
  elseif prX < newX then
    prX=prX-+
  end
  
  if prX > newX then
    prX=prX-1
  
  elseif prX < newX then
    prX=prX-+
  end
  
  
end